<h1> ゲーム詳細画面 </h1>

<%= "ゲームタイトル #{@game.name}" %>
<br>
<% Review.where(game_id: @game.id).each do |review| %>

<%= "タイトル #{review.title}" %><br>
<%= "ユーザー名 #{User.find(review.user_id).name} " %><br>
<%= "コメント #{review.comment}" %><br>

<br>
<% end %>

<br>